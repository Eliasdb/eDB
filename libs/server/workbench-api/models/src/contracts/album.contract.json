{
  "name": "album",
  "plural": "albums",
  "fieldsString": "title:string,status:enum(draft|published|archived),artistId:uuid,publishedYear?:number",
  "fields": [
    {
      "fieldName": "title",
      "required": true,
      "tsType": "string",
      "zodBase": "z.string()"
    },
    {
      "fieldName": "status",
      "required": true,
      "tsType": "'draft' | 'published' | 'archived'",
      "zodBase": "z.enum(['draft', 'published', 'archived'])"
    },

    {
      "fieldName": "artistId",
      "required": true,
      "tsType": "string",
      "zodBase": "z.string().uuid()"
    },
    {
      "fieldName": "publishedYear",
      "required": false,
      "tsType": "number",
      "zodBase": "z.number()"
    }
  ],
  "relationships": {
    "belongsTo": [
      {
        "name": "artist",
        "to": "artists",
        "fk": "artistId",
        "required": true,
        "include": true,
        "select": ["id", "name", "status"],
        "onDelete": "cascade"
      }
    ]
  }
}
