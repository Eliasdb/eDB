hub_list → GET /hub

hub_list_kind → GET /hub/:kind (with optional ?contactId= when kind=activities)

hub_activities_for_contact → GET /hub/contacts/:id/activities

hub_create → POST /hub/:kind

hub_update → PATCH /hub/:kind/:id

hub_delete → DELETE /hub/:kind/:id
