{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "workbenchApiModel generator",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Resource name (usually plural in CLI, e.g. 'books')",
      "pattern": "^[a-z0-9-]+$"
    },
    "fields": {
      "type": "string",
      "description": "Comma-separated fieldName:type. Supports ?:optional and enum(a|b). E.g. \"title:string,status:enum(draft|published|archived),publishedYear?:number\""
    }
  },
  "required": ["name", "fields"]
}
