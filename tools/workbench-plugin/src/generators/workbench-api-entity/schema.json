{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "workbenchApiEntity",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Plural-ish entity name used as route base (e.g., 'books')",
      "pattern": "^[a-z0-9-]+$"
    },
    "fields": {
      "type": "string",
      "description": "Comma-separated field specs: name:type (string|number|boolean|date|enum(a|b|c)|uuid). Optional with ?"
    },
    "api": { "type": "boolean", "default": true },
    "resource": { "type": "boolean", "default": true },
    "db": { "type": "boolean", "default": true }
  },
  "required": ["name", "fields"]
}
