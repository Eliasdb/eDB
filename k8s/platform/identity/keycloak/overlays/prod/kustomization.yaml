apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: auth
nameSuffix: -prod

resources:
  - ../../base

images:
  - name: eliasdb/keycloak-edb
    newTag: 26.2.4-v21-prod # <- your current prod tag

patches:
  - path: patch-env.yaml
