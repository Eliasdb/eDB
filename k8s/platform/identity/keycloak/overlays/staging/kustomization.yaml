apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: auth
nameSuffix: -staging

resources:
  - ../../base

images:
  - name: eliasdb/keycloak-edb
    newTag: 26.2.4-v20 # <- your current staging tag

patches:
  - path: patch-env.yaml
