apiVersion: apps/v1
kind: Deployment
metadata:
  name: tools-invoices-api
spec:
  template:
    spec:
      containers:
        - name: tools-invoices-api
          image: eliasdb/tools-invoices-api:latest # overridden by Image Updater
          env:
            - name: ENVIRONMENT
              value: 'staging'
