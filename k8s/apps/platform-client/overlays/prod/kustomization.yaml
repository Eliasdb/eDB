apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
nameSuffix: -prod
namespace: default
resources:
  - ../../base
patches:
  - path: patch-env.yaml
images:
  # Pin to an immutable prod tag
  - name: eliasdb/edb
    # newTag: v1.0.256-prod
