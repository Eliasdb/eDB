apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
nameSuffix: -prod
namespace: default
resources:
  - ../../base
patches:
  - patch.yaml
images:
  - name: eliasdb/admin-api
    newTag: ${IMAGE_TAG} # replaced by CI (or pinned manually if you prefer)
