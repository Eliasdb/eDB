apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: platform-client-prod
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: apps
  source:
    repoURL: https://github.com/Eliasdb/eDB.git
    targetRevision: main
    path: k8s/product/platform-client/overlays/prod
  destination:
    name: prod # 👈 now targets the prod cluster you registered
    namespace: default # or your prod namespace
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - PrunePropagationPolicy=foreground
      - PruneLast=true
