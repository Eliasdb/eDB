apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
nameSuffix: -prod
namespace: default
resources:
  - ../../base
patches:
  - path: patch.yaml
# No images: section here → prod tag is controlled in Git (or by your release process)
